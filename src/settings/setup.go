package settings

func SetupMelodex() {
	//fmt.Print("Enter playlist path: ")
	//scanner := bufio.NewScanner(os.Stdin)
	//if scanner.Scan() {
	//	playlistPath := scanner.Text()
	//}
	//
	//cache.GenerateCache(playlistPath)
}
